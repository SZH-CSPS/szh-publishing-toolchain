[
  // Exporter et régénérer l'aperçu PDF : la tâche de build par défaut.
  { "key": "ctrl+e", "command": "workbench.action.tasks.build" },

  // Importer à la demande les Word/LibreOffice déposés dans articles-word/
  {
    "key": "ctrl+alt+i",
    "command": "workbench.action.tasks.runTask",
    "args": "Importer les articles Word"
  },

  // Les trois panneaux du cockpit — Commande, Édition, Export — soit les mêmes menus que
  // les trois boutons de la barre « Revue SZH ». Le panneau d'édition contient toute la
  // palette « Mise en forme », qui reste aussi au clic droit.
  { "key": "ctrl+alt+a", "command": "szh.panneauCommande", "when": "szh.estRevue" },
  {
    "key": "ctrl+alt+s",
    "command": "szh.panneauEdition",
    "when": "szh.estRevue || (editorTextFocus && editorLangId == markdown)"
  },
  { "key": "ctrl+alt+d", "command": "szh.panneauExport", "when": "szh.estRevue" },
  { "key": "ctrl+alt+p", "command": "szh.basculerApercu", "when": "szh.estRevue" },

  // Mise en forme SZH, au niveau utilisateur, donc prioritaire sur Markdown All in One ;
  // ces raccourcis figurent aussi dans le sous-menu « Mise en forme » du clic droit.
  { "key": "ctrl+b",     "command": "szh.fmt.gras",     "when": "editorTextFocus && editorLangId == markdown" },
  { "key": "ctrl+i",     "command": "szh.fmt.italique", "when": "editorTextFocus && editorLangId == markdown" },
  { "key": "ctrl+u",     "command": "szh.fmt.souligne", "when": "editorTextFocus && editorLangId == markdown" },
  { "key": "ctrl+alt+1", "command": "szh.fmt.titre1",   "when": "editorTextFocus && editorLangId == markdown" },
  { "key": "ctrl+alt+2", "command": "szh.fmt.titre2",   "when": "editorTextFocus && editorLangId == markdown" },
  { "key": "ctrl+alt+3", "command": "szh.fmt.titre3",   "when": "editorTextFocus && editorLangId == markdown" },
  { "key": "ctrl+alt+w", "command": "szh.fmt.important", "when": "editorTextFocus && editorLangId == markdown" },
  { "key": "ctrl+alt+h", "command": "szh.fmt.highlight", "when": "editorTextFocus && editorLangId == markdown" },
  { "key": "ctrl+alt+q", "command": "szh.fmt.question",  "when": "editorTextFocus && editorLangId == markdown" },
  { "key": "ctrl+alt+c", "command": "szh.fmt.citation",  "when": "editorTextFocus && editorLangId == markdown" },
  { "key": "ctrl+alt+f", "command": "szh.fmt.figure",    "when": "editorTextFocus && editorLangId == markdown" },
  // « Insérer un tableau » : dans un article, crée articles/<slug>/tables/table-NN.html,
  // insère sa référence ::: {.szh-tabelle src="…"} au curseur et ouvre l'éditeur de
  // tableau. Hors article, repli sur un squelette Markdown.
  { "key": "ctrl+alt+t", "command": "szh.fmt.tableau",   "when": "editorTextFocus && editorLangId == markdown" },

  // Coller un tableau copié depuis Excel ou Word : le cockpit lit la variante HTML du
  // presse-papiers, la seule qui porte les cellules fusionnées, écrit
  // articles/<slug>/tables/table-NN.html et insère sa référence au curseur.
  {
    "key": "ctrl+alt+v",
    "command": "szh.fmt.collerTableau",
    "when": "editorTextFocus && editorLangId == markdown"
  },

  // Saut de page : ce qui suit repart en haut d'une page dans le PDF. Sans effet en
  // HTML, où le marqueur est inerte.
  {
    "key": "ctrl+alt+enter",
    "command": "szh.fmt.sautPage",
    "when": "editorTextFocus && editorLangId == markdown"
  },

  // Secours si l'aperçu PDF ne se rafraîchit pas tout seul : recharge la fenêtre.
  { "key": "ctrl+alt+r", "command": "workbench.action.reloadWindow" }

  // Ctrl+Maj+P (palette de commandes) reste actif : c'est le filet de secours.
]

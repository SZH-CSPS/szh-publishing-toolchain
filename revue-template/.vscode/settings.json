{
  // NB : la config triggerTaskOnSave.* a été déplacée dans revue.code-workspace
  // (portée « window » -> ignorée ici quand on ouvre via le .code-workspace).
  "search.exclude": { "out/**": true },
  "files.watcherExclude": { "**/out/**": true, "**/*.tmp": true }
}

# Todo Robin (après le lot M1–M5, 2026-08-13) :

- [ ] Redémarrer VSCodium (copies dev cockpit + szh-apercu déjà poussées), puis dérouler les
  scénarios GUI du bilan M (aperçu HTML cliquable, formulaire métadonnées, réglages, langue DE).
- [ ] Valider les libellés DE/IT des 6 types d'article (M1, table `LIBELLES_TYPES` de l'extension).
- [ ] Valider les traductions DE du cockpit (M4 — 102 clés `TEXTES_COCKPIT` + `package.nls.de.json`),
  premier jet non relu par un germanophone.
- [ ] Re-synchroniser `pipeline/` vers `C:\ProgramData\SZH\toolkit\pipeline\` (ou tagger une release)
  avant de tester l'import de tableaux (M2) et l'aperçu HTML (M5) sur le poste.
- [ ] Sortir `Feature.docx` du dépôt (public) — le déplacer vers OneDrive, comme les docx de `tmp/`.

# Todo Features :
Utilise le calculateur APCA pour recalculer les couleurs d'accentuations idéales dans les tableaux. 

Créé une palette complète de couleurs (plusirus gradation) au moins 3 utilisables avec l'écriture en blanc et 3 avec l'écriture en noir pour chaque couleur d'accentuation 

Créé un fichier html qui présente rapidement cette palette 